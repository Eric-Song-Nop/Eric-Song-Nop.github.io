---
title: Gocire generate docs with ide features does work for multiple languages
---
Gocire supports multiple programming languages, yes.
<details open="true">
<summary>Expand to view code</summary>
<pre className="cire"><code>
<span className="cire_text">{`class `}</span><Tooltip overlay={<div className="cire-markdown" dangerouslySetInnerHTML={{ __html: `<pre><code class="language-ts">class GoCire
</code></pre>
<p>Gocire supports multiple programming languages, yes.</p>
` }} />} placement="top" trigger={['hover']}><span id="scip-typescript+npm+ericoolen+0.0.0+blog/%60gocire-multilang.ts%60/GoCire%23" className="type">{`GoCire`}</span></Tooltip><span className="cire_text">{` {
    `}</span><Tooltip overlay={<div className="cire-markdown" dangerouslySetInnerHTML={{ __html: `<pre><code class="language-ts">constructor(parameters: any): GoCire
</code></pre>
<p>Gocire supports multiple programming languages, yes.</p>
` }} />} placement="top" trigger={['hover']}><span id="scip-typescript+npm+ericoolen+0.0.0+blog/%60gocire-multilang.ts%60/GoCire%23%60%3Cconstructor%3E%60%28%29." className="">{`constructor`}</span></Tooltip><span className="cire_text">{`(`}</span><Tooltip overlay={<div className="cire-markdown" dangerouslySetInnerHTML={{ __html: `<pre><code class="language-ts">(parameter) parameters: any
</code></pre>
` }} />} placement="top" trigger={['hover']}><span id="scip-typescript+npm+ericoolen+0.0.0+blog/%60gocire-multilang.ts%60/GoCire%23%60%3Cconstructor%3E%60%28%29.%28parameters%29" className="variable.parameter">{`parameters`}</span></Tooltip><span className="cire_text">{`) {
    }
}
`}</span></code></pre>
</details>
